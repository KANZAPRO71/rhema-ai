# Salin ke android/keystore.properties (file ini TIDAK di-commit).
# Buat keystore: lihat scripts/build-play-release.ps1

storeFile=../release/rhema-release.keystore
storePassword=GANTI_DENGAN_PASSWORD_KUAT
keyAlias=rhema-release
keyPassword=GANTI_DENGAN_PASSWORD_KUAT
